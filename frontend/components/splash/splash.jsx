import React from 'react';
import { Link } from 'react-router-dom';

export default class Splash extends React.Component {
  render() {
    return(
      <div>
        <header className='SplashNavBar'>
          <h1>Blackbird (insert logo)</h1>
          {/* login link */}
          {/* signup link */}
        </header>
      </div>
    );
  }
}